[package]
name = "fedora-aspace-lambda"
version = "0.1.0"
edition = "2024"

[dependencies]
fedora-tools = { path = "../fedora-tools"}
futures = "0.3.33"
tokio = {version = "1.53.1", features = ["full"]}
tokio-stream = {version = "0.1.19", features = ["full"]}
